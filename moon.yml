$schema: 'https://moonrepo.dev/schemas/project.json'
layer: 'library'
tags: ['mojo']

tasks:
  check:
    command: 'pixi run test'
    toolchain: 'system'
    inputs: ['src/**/*.mojo', 'test/**/*', 'tests/**/*', 'pixi.toml', 'pixi.lock']
